/**
 * PascalCase alias — resolves imports from pages that use:
 * import { HeroSection } from '@/components/molecules/HeroSection'
 */
export { HeroSection, type HeroSectionProps, type HeroAction } from './hero-section-main';
